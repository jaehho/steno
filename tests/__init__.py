"""Headless tests.

Nothing here opens audio, reaches the network, or needs a display — the engine
is deliberately separable from both the GUI and the services it calls, and
these exist to keep it that way.
"""
